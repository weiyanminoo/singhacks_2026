# alternate.ai — approved dependencies only. Ask before adding anything here.
fastapi
uvicorn
httpx
xrpl-py
python-dotenv
openai
pytest
pyyaml

# x402 — resolved (D-010): `x402-xrpl` is the XRPL payment scheme + FastAPI
# middleware. NOT `x402-secure`, which is t54's Trustline risk/proxy SDK.
x402-xrpl
